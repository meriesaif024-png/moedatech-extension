// Copy this file to config.js and fill in the real API key (get it from whoever manages the backend).
// config.js is gitignored and must never be committed.
const API_KEY = "PASTE_THE_REAL_API_KEY_HERE";
